{
  "name": "monkeybook",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20 <23"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\" \"npm run dev:worker\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "EMBEDDED_WORKER=false node server/index.js",
    "dev:worker": "node server/worker.js",
    "build": "npm install --prefix client --include=dev && npm install --prefix server && npm run build --prefix client",
    "start": "sh scripts/start.sh",
    "start:worker": "node server/worker.js",
    "start:autoscaler": "node autoscaler/index.js",
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "test:client": "npm run test --prefix client",
    "test:server": "npm run test --prefix server",
    "test": "npm run test:server && npm run test:client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
